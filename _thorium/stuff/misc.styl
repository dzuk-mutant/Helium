
antialiased-fonts()
	// makes sure that fonts look nice on macOS.
	-webkit-font-smoothing: antialiased
	-moz-osx-font-smoothing: grayscale


text-overflow-ellipsis()
	// makes sure the text doesn't wrap to new line
	white-space: nowrap

	// makes sure that horizontal overflow is cut off in the container
	overflow-x: hidden

	// adds the ellipsis
	text-overflow: ellipsis
