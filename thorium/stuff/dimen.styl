


//	size

/* 	use this to create quick sizes for other things.

		icon class example:
		.size-9 { size(9px) }
*/

size-sq(dimen)
	width: dimen
	height: dimen

size-xy(h, w)
	width: w
	height: h

size()
	len = length(arguments)

	if len == 2
		size-xy(arguments[0], arguments[1])

	else if len == 1
		size-sq(arguments[0])

	else
		xy-length-error("size()", len)







//	size-reduce

/* 	clears the padding you make in icons and other objects so they can be
		set to baselines and keylines.

		Set the number of the name of the resulting class
		to the same as the icon set size.

		eg:

		.size-25 { size(25px) }
		.size-25.reduced { size(25px) size-reduce(5px) }

		<span class='ic-arrow-left size-25 reduced'></span>
*/

size-reduce-sq(dimen)
	margin: -dimen

size-reduce-xy(h, w)
	margin: -h -w

size-reduce()
	len = length(arguments)

	if len == 2
		size-reduce-xy(arguments[0], arguments[1])

	else if len == 1
		size-reduce-sq(arguments[0])

	else
		xy-length-error("size-reduce()", len)
